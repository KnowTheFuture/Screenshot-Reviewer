restartuvicorn() {
  echo "💀 Killing old uvicorn processes on :8000..."
  pkill -f "uvicorn" && sleep 1
  echo "🚀 Restarting uvicorn..."
  uvicorn backend.main:app --reload --host 0.0.0.0 --port 8000
}
